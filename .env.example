# GCP Cloud Run/API Gateway base URL that serves the landing API routes.
# Leave empty only when this frontend is deployed behind the same origin as the API.
VITE_LANDING_API_BASE_URL=https://estospaces-landing-api.example.run.app
